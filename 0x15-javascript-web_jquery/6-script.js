$('document').ready(function () {
  // Add a click event to DIV#update_header
  $('#update_header').click(function () {
    // Update the text of <header> to  "New Header!!!"
    $('header').text('New Header!!!');
  });
});
